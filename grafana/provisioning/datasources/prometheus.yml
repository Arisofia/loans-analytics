apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Internal Docker network URL — matches the 'prometheus' service in docker-compose.yml
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "30s"
