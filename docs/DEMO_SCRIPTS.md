# Demo Scripts

## Financial Analysis Demo

- Script: `scripts/demo_financial_analysis.py`
- Requirements: `pandas` is required; plotting uses `matplotlib` when available.
- Behavior: if `matplotlib` is not installed, the script runs and skips plots.
