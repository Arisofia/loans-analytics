agents:
  kpi_analytics:
    refresh_schedule:
      continuous_monitoring: '*/5 * * * *'
  risk_analysis:
    refresh_schedule:
      continuous_monitoring: '*/10 * * * *'
integrations:
  meta:
    enabled: false
  perplexity_comet:
    enabled: false
observability:
  logging:
    level: DEBUG
pipeline:
  phases:
    ingestion:
      source: 
      validation:
        strict: true
    outputs:
      storage:
        local_dir: data/metrics/dev
      supabase:
        enabled: false
run:
  raw_archive_dir: data/archives/_exports/archive
