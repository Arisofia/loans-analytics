"""Decision-grade agents — analytical agents that consume marts/metrics.

This package supplements the existing LLM-based agents in
``backend.loans_analytics.multi_agent.agents`` (which is ``agents.py``).
The new *decision agents* live under ``agents/`` sub-package.
"""
