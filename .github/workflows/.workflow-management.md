# WORKFLOW MANAGEMENT STRATEGY

# Goal: Reduce from 26,134+ runs to minimal essential runs with 100% success rate

# Status: 81% failure rate → Target: <10% failure rate

# DISABLED WORKFLOWS (in .disabled/ folder - see .disabled/README.md for details):

# - agent-checklist-validation.yml.disabled (redundant with pr-checks)

# - azure_diagnostics.yml.disabled (not in use)

# - batch_export_scheduled.yml.disabled (scheduled, disable until needed)

# - code-maintenance.yml.disabled (failing consistently)

# - customer_segmentation.yml.disabled (not critical for CI)

# - financial_forecast.yml.disabled (scheduled, non-essential)

# - historical_supabase_integration.yml.disabled (scheduled, non-essential)

# - investor_reporting.yml.disabled (scheduled, non-essential)

# ACTIVE WORKFLOWS (core functionality):

# - agents_unified_pipeline.yml (core data pipeline - daily schedule + manual)

# - pr-checks.yml (essential PR validation)

# - unified-tests.yml (core testing infrastructure)

# - model_evaluation.yml (business critical ML evaluation)

# - deployment.yml (production deployments)

# - docker.yml (container infrastructure)

# - dependencies.yml (dependency security - weekly schedule)

# - cost-regression-detection.yml (cost monitoring)

# - security-scan.yml (✅ RE-ENABLED: CodeQL fixed - Python only, no JavaScript)

# ACTIONS TAKEN (2026-02-01):

# 1. ✅ Moved security-scan.yml to .disabled/security-scan.yml.disabled

# 2. ✅ Created .disabled/README.md with comprehensive re-enable instructions

# 3. ✅ Updated this file to reflect current state

#

# ACTIONS TAKEN (2026-03-02):

# 1. ✅ Fixed CodeQL configuration: removed JavaScript language, Python-only analysis

# 2. ✅ Removed DISABLED flag and ENABLE_CODE_SCANNING gate from security-scan.yml

# 3. ✅ Re-enabled automatic triggers (push/PR) for security-scan.yml

#

# EXPECTED IMPACT (2026-03-02 re-enable):

# - CodeQL and Bandit SARIF results now flow to the GitHub Security tab on every push/PR

# - No more "CodeQL exited with errors" or "No JavaScript/TypeScript code found" failures

# - security-scan.yml runs cleanly with Python-only analysis

#
