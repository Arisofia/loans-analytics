name: Codex Pull Request Review

"on":
  workflow_call:
    secrets:
      OPENAI_API_KEY:
        required: false
<<<<<<< HEAD
    inputs:
      is_fork:
        description: Whether the pull request originates from a fork.
        required: true
        type: boolean
=======
>>>>>>> origin/main

jobs:
  assign_metadata:
    runs-on: ubuntu-latest
<<<<<<< HEAD
    if: ${{ inputs.is_fork == false && secrets.OPENAI_API_KEY != '' }}
=======
    if: secrets.OPENAI_API_KEY != ''
>>>>>>> origin/main
    permissions:
      pull-requests: write
    steps:
      - name: Assign PR reviewers and assignees
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const pullNumber = context.payload.pull_request.number;
            await github.rest.issues.addAssignees({
              owner,
              repo,
              issue_number: pullNumber,
              assignees: ['codex'],
            });
  codex_review:
    name: Run Codex Review
    needs: assign_metadata
    runs-on: ubuntu-latest
<<<<<<< HEAD
    if: ${{ secrets.OPENAI_API_KEY != '' }}
=======
    if: secrets.OPENAI_API_KEY != ''
>>>>>>> origin/main

    permissions:
      contents: read
      pull-requests: write
    outputs:
      final_message: ${{ steps.run_codex.outputs['final-message'] }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refs/pull/${{ github.event.pull_request.number }}/merge
      - name: Run Codex
        id: run_codex
        continue-on-error: true
        uses: openai/codex-action@v1
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}
          prompt-file: .github/codex/prompts/review.md
          output-file: codex-output.md
          safety-strategy: drop-sudo
          sandbox: workspace-write

  post_feedback:
    name: Post Codex Feedback
    needs: codex_review
    runs-on: ubuntu-latest
<<<<<<< HEAD
    if: ${{ inputs.is_fork == false && secrets.OPENAI_API_KEY != '' && needs.codex_review.outputs.final_message != '' }}
=======
    if: github.event.pull_request.head.repo.fork == false && secrets.OPENAI_API_KEY != '' && needs.codex_review.outputs.final_message != ''
>>>>>>> origin/main
    permissions:
      pull-requests: write
    steps:
      - name: Post feedback to PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            const finalMessage = `${{ needs.codex_review.outputs.final_message }}`;
            if (finalMessage) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                body: finalMessage,
              });
            name: Codex PR Review

            on:
              workflow_call:
                inputs:
                  is_fork:
                    description: 'Whether the PR is from a fork'
                    required: true
                    type: boolean
                secrets:
                  OPENAI_API_KEY:
                    required: true

            jobs:
              codex-review:
                name: Codex PR Review
                runs-on: ubuntu-latest
                if: ${{ inputs.is_fork == false }}
                steps:
                  - uses: actions/checkout@v4
                    with:
                      fetch-depth: 0

                  - name: Set up Python
                    uses: actions/setup-python@v5
                    with:
                      python-version: '3.11'

                  - name: Install dependencies
                    run: pip install -r requirements-dev.txt

                  - name: Run Codex PR analysis
                    env:
                      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
                      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                    run: |
                      python -m tools.pr_review_codex
