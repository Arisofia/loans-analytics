<<<<<<< HEAD
name: codeql
=======
name: CodeQL Analysis

>>>>>>> main
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
<<<<<<< HEAD
    - cron: "0 3 * * 1"
  workflow_dispatch:
=======
    - cron: '0 0 * * 0'
  workflow_dispatch:

<<<<<<< HEAD
concurrency:
  group: codeql-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
=======
>>>>>>> main
>>>>>>> main
permissions:
  actions: read
  contents: read
  security-events: write
jobs:
  analyze:
<<<<<<< HEAD
=======
    name: Analyze (${{ matrix.language }})
>>>>>>> main
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
      pull-requests: read
    strategy:
      fail-fast: false
      matrix:
<<<<<<< HEAD
        language: ["javascript", "python", "csharp"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Detect C# projects
        id: detect_csharp
        run: |
          has_csharp=$(python - <<'PY'
import pathlib

root = pathlib.Path('.')
has_projects = any(root.rglob('*.csproj')) or any(root.rglob('*.sln'))
print('true' if has_projects else 'false')
PY
)
          echo "has_csharp=${has_csharp}" >> "$GITHUB_OUTPUT"

      - name: Set up .NET
        if: matrix.language == 'csharp' && steps.detect_csharp.outputs.has_csharp == 'true'
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Set up CodeQL
        if: matrix.language != 'csharp' || steps.detect_csharp.outputs.has_csharp == 'true'
        uses: github/codeql-action/init@v3.31.5
=======
        language: ['javascript', 'python']
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
<<<<<<< HEAD
      - name: Set up CodeQL
=======
        with:
          fetch-depth: 0

      - name: Setup Node.js
        if: matrix.language == 'javascript'
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Python
        if: matrix.language == 'python'
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Initialize CodeQL
>>>>>>> main
        uses: github/codeql-action/init@v3
>>>>>>> main
        with:
          languages: ${{ matrix.language }}
      - name: Autobuild
<<<<<<< HEAD
        if: matrix.language != 'csharp' || steps.detect_csharp.outputs.has_csharp == 'true'
        uses: github/codeql-action/autobuild@v3.31.5

      - name: Perform CodeQL Analysis
        if: matrix.language != 'csharp' || steps.detect_csharp.outputs.has_csharp == 'true'
        uses: github/codeql-action/analyze@v3.31.5
        with:
          category: "/language:${{ matrix.language }}"
=======
<<<<<<< HEAD
        uses: github/codeql-action/autobuild@v3.25.6
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3.25.6
        with:
          category: "/language:${{ matrix.language }}"
=======
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: '/language:${{ matrix.language }}'
>>>>>>> main
>>>>>>> main
