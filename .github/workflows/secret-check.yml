name: Secret check

on:
    workflow_dispatch:

jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - name: Verify MIDDLEWARE_SHARED_SECRET exists
              shell: bash
              env:
                  MIDDLEWARE_SHARED_SECRET: ${{ secrets.MIDDLEWARE_SHARED_SECRET }}
              run: |
                  set -euo pipefail
                  if [ -z "$MIDDLEWARE_SHARED_SECRET" ]; then
                    echo "Missing required secret: MIDDLEWARE_SHARED_SECRET" >&2
                    exit 1
                  fi
                  echo "MIDDLEWARE_SHARED_SECRET present"
