name: Minimal CI Test

on:
  push:
    branches: [main, develop, fix/ci-workflow-codecov]
  pull_request:
    branches: [main, develop, fix/ci-workflow-codecov]
  workflow_dispatch:

jobs:
permissions:
  contents: read

# View Visual
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: echo "Hello, world!"
