name: SonarCloud
permissions:
  contents: read
  pull-requests: read
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
jobs:
  sonarcloud:
    name: SonarCloud Scan
    permissions:
      contents: read
      pull-requests: read
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_GITHUB }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout repository
        if: steps.check-secrets.outputs.secrets_available == true
        uses: actions/checkout@v4
        with:
          # Necessary for SonarCloud to analyze history
          fetch-depth: 0
      - name: Set up pnpm
        if: steps.check-secrets.outputs.secrets_available == true
        uses: pnpm/action-setup@v3
        with:
          version: 10
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: apps/web/pnpm-lock.yaml
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        if: steps.check-secrets.outputs.secrets_available == true
        run: |
          cd apps/web
          npm install

      - name: Analytics coverage (pytest)
        if: steps.check-secrets.outputs.secrets_available == true
        run: |
          python -m pip install -r apps/analytics/requirements.txt pytest pytest-cov
          cd apps/analytics
          pytest --cov=. --cov-report=xml:../coverage-python.xml

      - name: Java coverage (Jacoco)
        run: |
          if find . -name "*.gradle" -o -name "*.gradle.kts" | grep -q .; then
            if [ -x ./gradlew ]; then
              ./gradlew jacocoTestReport
            else
              echo "Gradle build files found but gradlew not present or not executable; skipping Jacoco"
            fi
          else
            echo "Skipping Jacoco: no Gradle build files found in repository"
          fi

      - name: Generate web coverage (Jest)
        run: |
          cd apps/web
          npm run test -- --coverage --coverageReporters=lcov
          # The lcov.info file will be generated at coverage/lcov.info

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@e44258b109568baa0df60ed515909fc6c72cba92
        with:
          args: >
            -Dsonar.verbose=true
            -Dsonar.python.coverage.reportPaths=coverage-python.xml
